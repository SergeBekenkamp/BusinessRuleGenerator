-- TRIGGER FOR ENTITY OTHER RULE (EOTH)

-- Comments: <<comment>>

CREATE OR REPLACE TRIGGER <<trigger_name>>
BEFORE <<trigger_event>>
OF <<col_name>> <<second_col_name>> ON <<table_name>>
FOR EACH ROW
declare
  l_passed boolean := true;
  l_error_stack varchar2(4000);
BEGIN
  l_passed := <<customsql>>;
  if not l_passed then
   	<<error>>
    l_error_stack := l_error_stack || 'The <<customsql>> code returned an error.';
    raise_application_error(-20000, l_error_stack);
  end if;  
END; 